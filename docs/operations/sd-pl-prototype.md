# PL SD через SPI Master

FreeRTOS-приложение BVSTK работает с внешней SD-картой через существующее
ядро SPI Master и AXI DMA. RTL не изменяется. Драйвер и служба PL SD входят
в обычную сборку `app_bvstk.elf`; отдельный ELF и отдельный Makefile для SD
не используются. Поверх проверенных операций с секторами подключена FatFs:
том `sd-pl:/` (внутри `2:/`). Это не штатный PS SD-слот AX7020 (`sd:/`).

Текущий режим предназначен для SDHC/SDXC с секторной адресацией. Карта
инициализируется и монтируется автоматически при каждом старте программы.
При монтировании носитель только читается. Используется существующая
FAT12/16/32; форматирование и восстановление разметки не выполняются.
Поддерживается superfloppy либо первый подходящий первичный раздел MBR.
exFAT, GPT и расширенные разделы не поддерживаются. При отсутствии подходящей
ФС том остаётся недоступен, остальные службы продолжают работать.

## Подключение

| J10 | Контакт FPGA | Сигнал модуля |
|---:|---|---|
| 2 | питание 5 В | VCC внешнего модуля с соответствующим питанием |
| 4 | W18 | MOSI |
| 6 | P14 | MISO |
| 8 | Y16 | CS |
| 10 | V15 | SCK |

Обязательна общая земля. Сигналы FPGA — LVCMOS33; 5 В относятся к питанию
подходящего модуля, а не к уровням его SPI. Рабочий UART стенда:
`/dev/ttyUSB0`, CP2102N, 115200 8N1, без аппаратного управления потоком.
Имя устройства проверяется через `/dev/serial/by-id/`.

## Сборка

PL SD собирается вместе со всеми службами BVSTK:

```sh
./build.sh check
./build.sh freertos
```

Результат: `vitis_ws/app_bvstk/Debug/app_bvstk.elf`. Состав исходников
формируется `scripts/vitis/build.tcl`, поэтому драйвер, FreeRTOS-порт,
служба и UART-консоль компилируются в том же приложении, что и сеть,
файловые службы и остальные команды. `BVSTK_PL_SPI_DIAGNOSTIC=1` остаётся
только явным режимом аппаратной диагностики PL SPI; обычная сборка этот
режим не включает.

## Загрузка

На плате с соответствующими PS/DDR и PL загрузите обычное приложение:

```sh
ELF_FILE="$PWD/vitis_ws/app_bvstk/Debug/app_bvstk.elf" \
  ./scripts/vitis/run_jtag.sh
```

Сценарий программирует выбранный существующий bitstream, выполняет PS7 init
и загружает полный ELF в DDR. Он не собирает и не изменяет RTL/bitstream и
не прошивает постоянную flash. Переопределение ELF — `ELF_FILE`.

Если PL ещё не загружена, предварительно загрузите **готовый неизменённый**
bitstream из того же XSA и выполните соответствующий `ps7_init`. Например,
для текущего комплекта, после проверки контрольных сумм:

```sh
BITSTREAM_FILE="$PWD/artifacts/fpga/design.bit" \
ELF_FILE="$PWD/vitis_ws/app_bvstk/Debug/app_bvstk.elf" \
PS7_INIT_TCL="$PWD/vitis_ws/plat_bvstk/hw/ps7_init.tcl" \
  ./scripts/vitis/run_jtag.sh
```

Последний сценарий сбрасывает работающую систему и программирует PL
существующим файлом. Это не сборка/редактирование RTL. Постоянную flash
он не прошивает. Подробнее — [JTAG](../build/jtag.md).

## Команды

После загрузки в UART должно появиться:

```text
SD-PL: mounted sd-pl:/ (read/write, existing filesystem)
```

Для работы с уже имеющимися данными:

```text
sd-pl info
ls sd-pl:/
cd sd-pl:/
ls
cat roundtrip.json
```

Имя существующего файла в примере замените на нужное. `sd-pl info` должен
показать `mounted=1`. Приглашение может первоначально оставаться `bvstk/sd>`:
оно указывает текущий том консоли, а не единственный смонтированный том.
После `cd sd-pl:/` приглашение становится `bvstk/sd-pl>`; `pwd` выводит
внутренний путь `2:/`.

Пример записи в **новый выбранный вами** каталог:

```text
mkdir sd-pl:/demo
fs write sd-pl:/demo/hello.txt Hello
fs append sd-pl:/demo/hello.txt , world!
cat sd-pl:/demo/hello.txt
cp sd-pl:/demo/hello.txt sd-pl:/demo/copy.txt
mv sd-pl:/demo/copy.txt sd-pl:/demo/renamed.txt
ls sd-pl:/demo
```

`fs write` заменяет содержимое существующего файла, `fs append` дописывает.
Текст — остаток командной строки, без обработки кавычек/escape и без
автоматического перевода строки. UART имеет редактор строки с историей,
стрелками и `Tab`; редактор хранит до 511 символов, но общий диспетчер
разбирает первые 255 символов команды. Для длинных/двоичных файлов
используйте HTTP-загрузку или копирование с другого тома (SSH/SCP — при
включённой SSH-сборке). При вставке длинной команды в minicom задайте
задержку символов: консоль опрашивает UART без управления потоком. Тестовый
скрипт ниже уже дозирует передачу.
`rm` удаляет файл/пустой каталог, `rm -r` — дерево. `touch` усекает файл.

Сырые `sd-pl init/read/write/test/status/blocklen` запрещены, пока картой
владеет файловая подсистема, включая случай неудачного монтирования.
Это защита ФС от прямой записи в её сектора. `sd-pl info` доступна.
`fs format sd-pl confirm` всегда отклоняется в данном профиле.
После ошибки I/O/CRC/тайм-аута том недоступен до перезапуска системы;
повторная запись и скрытое перемонтирование не выполняются. Извлекать и
заменять карту следует только при выключенном устройстве.

## Особенности текущего ядра

1. Mailbox CMD17 меняется при чтении и заканчивается значением `0xFF`.
   Он не используется как зафиксированный R1. Профиль AX7020 разрешает
   этот **конкретный** результат только при свежем IRQ, чистых завершённых
   DMA, длине 514, целых защитных границах и совпадении CRC16. Для CMD13,
   CMD16 и CMD24 ответ по-прежнему должен быть нулевым.
2. Прямая цепочка команд после CMD17 на проверенном комплекте зависала.
   Поэтому сервис выполняет полную инициализацию **перед каждой следующей
   командой**, включая чтение для проверки записи. Число таких подготовок
   выводится в отчёте. Это медленный совместимый режим, не исправление PL
   и не автоматический повтор неудачной записи.
3. SDSC в этом профиле отклоняется как неподтверждённый вариант. Нет
   определения физической ёмкости/CSD, CMD18/CMD25, стирания и порта Нейтрино.
   Число секторов в `info` — проверенный размер существующего FAT-тома.
4. Каждая запись сектора включает полное чтение и сравнение 512 байт.
   Поэтому файловая запись медленная; FAT не журналируется, а потеря питания
   при обновлении метаданных может повредить ФС. Одновременная запись одного
   файла через разные интерфейсы и горячая замена карты не поддерживаются.

Подробности интерфейса и API — [драйвер SD](../development/drivers/sd.md).

## Автоматическая проверка с компьютера

Закройте minicom/другие читатели UART. Нужны Python 3 (только стандартная
библиотека) и доступ к последовательному порту. Проверка без записи:

```sh
python3 tools/sd_pl_smoke.py --port /dev/ttyUSB0
```

Файловая проверка создаёт отдельный каталог `sd-pl:/bvstk-fs-<дата-время-pid>`
и записывает тестовые файлы только туда. Она не форматирует карту:

```sh
python3 tools/sd_pl_smoke.py --port /dev/ttyUSB0 --load \
  --fs-test --quiet --log build/sd-pl/fs-smoke.log
```

Проверяются создание каталогов и файла 4800 байт (дополнение, переход через
границы секторов/4-КиБ кластера), точное содержимое при чтении, копирование,
переименование, усечение, пустой файл/удаление, запрет raw-команд и формата.
`--load` загружает полный ELF с существующим bitstream до теста и повторно
после записи, затем проверяет сохранность файлов. Без этого флага тест
работает с уже запущенной системой, без проверки перезапуска.
Тестовые файлы остаются в указанном в `HOST PASS` каталоге.
`--command '...'` только записывает ответы консоли, не оценивая успех команды;
код 0 в этом режиме не означает успешность файловых операций.
Старый `--write-test` предназначен для raw-профиля и не допускается в
обычной сборке с автоматически монтируемой ФС.

## Аппаратный результат 21.09.2026

На текущей карте обнаружен FAT32 superfloppy: LBA 0, 739328 секторов
по 512 байт, кластер 4096 байт. В штатном полном ELF подтверждены автоматическое
монтирование, чтение существующего `roundtrip.json` и каталога `test1`,
весь файловый smoke-тест выше и сохранность тестовых файлов после сброса
PS/повторной загрузки существующего bitstream и ELF. Форматирования не было.
Логи в `build/sd-pl/`: `fs-mount.log`, `fs-original-files.log`, `fs-smoke.log`,
`fs-final-smoke.log`. Тестовый файл 4800 байт после копирования/перезапуска
оставлен в `sd-pl:/bvstk-fs-20260921-125729-544094/subdir/moved.txt`.
После окончательной проверки ошибок монтирования пересобран и загружен
ELF SHA-256 `b21e07470f098b9c6f741cd1ab290f713146a5177e8c3d541be81eb511de211d`.
`fs-final-boot.log`/`fs-final-review.log` подтверждают автомонтирование,
неизменность прочитанного до тестов `roundtrip.json`, точное сохранение
4800-байтного файла и новую запись/дополнение/чтение `final.txt`.
Это не тест снятия питания во время записи.

Ниже — ранее выполненная квалификация **сырого** драйвера до включения ФС:

На AX7020 с подключением выше получен OCR `C0FF8080`. Проверены CMD13,
CMD16, чтение CMD17, запись CMD24 и тест с последующей проверкой на хосте.
Диапазон 4096..4103, seed `89ABCDEF`: все 8 секторов совпали, соседние
4095/4104 не изменились. Повторный прогон на 8192..8199 с seed `FEDCBA98`
также прошёл, соседние 8191/8200 сохранены. Указанные тестовые диапазоны
перезаписаны, их прежнее содержимое не восстанавливалось.
Это проверка конкретной карты и комплекта, не
заявление о совместимости со всеми картами и не тест отключения питания.

Входные файлы (SHA-256):

```text
design.bit  b8ae821df67269556fe3bb471789372f4f037f1504a88babf846a15ebc431d1f
design.xsa  0b77d71e589dd44ce2249df7d6f0d18ca7e196c6313721d91e8fd778e2e20f8e
```

Bitstream внутри XSA и копия BSP совпадают с `artifacts/fpga/design.bit`.
Не использовался отличный от них `/home/ilya/Zynq/borya/borya.bit`.
Журналы аппаратной проверки могут быть сохранены в `build/sd-pl/`:
`uart-mailbox.log`, `uart-sequences.log`, `uart-descriptor16.log` фиксируют
исходные ограничения; `uart-isolated.log`, `uart-range8.log`,
`uart-final-range8.log` — успешные проверки совместимого режима.
`uart-final-status.log` фиксирует финальную цепочку
status → blocklen → write → read → info без ошибок.
Конкретный SHA-256 ELF зависит от текущего полного набора исходников и
параметров Vitis; проверяйте его у `vitis_ws/app_bvstk/Debug/app_bvstk.elf`.
Производные логи и ELF не являются исходниками и могут отсутствовать в
другом checkout.
