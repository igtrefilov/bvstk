# Device Communication Protocol v2 (DCP2) — протокол внешнего клиента по TCP

## Оглавление

- [Часть I. Концепция и принципы работы DCP2](#toc-001)
  - [1. Назначение протокола](#toc-002)
  - [2. Архитектура протокола](#toc-003)
    - [2.1. Заголовок фрейма `DCP2_HDR`](#toc-004)
    - [2.2. Полезная нагрузка фрейма `DCP2_PAYLOAD`](#toc-005)
    - [2.3. Карта структур DCP2](#toc-006)
  - [3. Модель обмена](#toc-007)
    - [3.1. Участники и роли](#toc-008)
    - [3.2. Типы сообщений](#toc-009)
    - [3.3. Корреляция сообщений](#toc-010)
    - [3.4. Модель ошибок](#toc-011)
    - [3.5. Типовые сценарии обмена](#toc-012)
      - [3.5.1. Корректный `request` с успешным ответом](#toc-013)
      - [3.5.2. Неподдерживаемый сервис или операция](#toc-014)
      - [3.5.3. Ошибка параметров запроса](#toc-015)
      - [3.5.4. Обработка входящего `event`](#toc-016)
  - [4. Расширяемость и совместимость](#toc-017)
  - [5. Нормативные правила интерпретации](#toc-018)
    - [5.1. Порядок байтов](#toc-019)
    - [5.2. Поведение при ошибках фрейминга](#toc-020)
    - [5.3. Глобальные ограничения длины](#toc-021)
    - [5.4. Правила использования `seq`](#toc-022)
    - [5.5. Таймауты и повторные попытки](#toc-023)
    - [5.6. Обнаружение возможностей сервера](#toc-023a)
    - [5.7. Политика версионирования](#toc-023b)
- [Часть II. Формальная спецификация DCP2](#toc-024)
- [1. Структура DCP2_HDR](#toc-025)
  - [1.1. Поле `magic`](#toc-026)
  - [1.2. Поле `ver`](#toc-027)
  - [1.3. Поле `dcp_len`](#toc-028)
- [2. Структура `DCP2_PAYLOAD`](#toc-029)
  - [2.1. Поле `srv`](#toc-030)
  - [2.2. Структура `SRV_MESSAGE`](#toc-031)
    - [2.2.1. Поле `op`](#toc-032)
    - [2.2.2. Поле `seq`](#toc-033)
    - [2.2.3. Структура `SRV_REQUEST_BODY`](#toc-034)
      - [2.2.3.1 Структура `PING_REQUEST`](#toc-035)
      - [2.2.3.2 Структура `MEM_READ_REQUEST`](#toc-036)
        - [2.2.3.2.1. Поле `flags`](#toc-037)
        - [2.2.3.2.2. Поле `width`](#toc-038)
        - [2.2.3.2.3. Поле `addr`](#toc-039)
      - [2.2.3.3. Структура `MEM_WRITE_REQUEST`](#toc-040)
      - [2.2.3.4. Структура `I2C_READ_REG_REQUEST`](#toc-041)
      - [2.2.3.5. Структура `I2C_WRITE_REG_REQUEST`](#toc-042)
      - [2.2.3.6. Структура `I2C_POLICY_SET_REQUEST`](#toc-043)
        - [2.2.3.6.1. Поле `policy`](#toc-044)
      - [2.2.3.7. Структура `SMI_READ_REQUEST`](#toc-045)
      - [2.2.3.8. Структура `SMI_WRITE_REQUEST`](#toc-046)
      - [2.2.3.9. Структура `NOTIFY_SUBSCRIBE_REQUEST`](#toc-046a)
        - [2.2.3.9.1. Поле `class_mask`](#toc-046b)
        - [2.2.3.9.2. Поле `source_mask`](#toc-046c)
        - [2.2.3.9.3. Поле `bus_mask`](#toc-046d)
        - [2.2.3.9.4. Поле `flags`](#toc-046e)
      - [2.2.3.10. Структура `NOTIFY_UNSUBSCRIBE_REQUEST`](#toc-046f)
      - [2.2.3.11. Структура `PL_SUBSCRIBE_STREAM_REQUEST`](#toc-047)
        - [2.2.3.11.1. Поле `flags`](#toc-048)
      - [2.2.3.12. Структура `PL_UNSUBSCRIBE_STREAM_REQUEST`](#toc-049)
      - [2.2.3.13. Структуры FS-запросов](#toc-050a)
        - [2.2.3.13.1. `FS_INFO_REQUEST`](#toc-050b)
        - [2.2.3.13.2. `FS_STAT_REQUEST`](#toc-050c)
        - [2.2.3.13.3. `FS_OPEN_REQUEST`](#toc-050d)
        - [2.2.3.13.4. `FS_READ_REQUEST`](#toc-050e)
        - [2.2.3.13.5. `FS_CLOSE_REQUEST`](#toc-050f)
    - [2.2.4. Структура `SRV_RESPONSE_BODY`](#toc-051)
      - [2.2.4.1. Поле `status`](#toc-052)
      - [2.2.4.2. Структура `PING_RESPONSE`](#toc-053)
      - [2.2.4.3. Структура `MEM_READ_RESPONSE`](#toc-054)
      - [2.2.4.4. Структура `MEM_WRITE_RESPONSE`](#toc-055)
      - [2.2.4.5. Структура `I2C_READ_REG_RESPONSE`](#toc-056)
      - [2.2.4.6. Структура `I2C_WRITE_REG_RESPONSE`](#toc-057)
      - [2.2.4.7. Структура `I2C_POLICY_SET_RESPONSE`](#toc-058)
      - [2.2.4.8. Структура `SMI_READ_RESPONSE`](#toc-059)
      - [2.2.4.9. Структура `SMI_WRITE_RESPONSE`](#toc-060)
      - [2.2.4.10. Структура `NOTIFY_SUBSCRIBE_RESPONSE`](#toc-060a)
      - [2.2.4.11. Структура `NOTIFY_UNSUBSCRIBE_RESPONSE`](#toc-060b)
      - [2.2.4.12. Структура `PL_SUBSCRIBE_STREAM_RESPONSE`](#toc-061)
      - [2.2.4.13. Структура `PL_UNSUBSCRIBE_STREAM_RESPONSE`](#toc-062)
      - [2.2.4.14. Структуры FS-ответов](#toc-063a)
        - [2.2.4.14.1. `FS_INFO_RESPONSE`](#toc-063b)
        - [2.2.4.14.2. `FS_STAT_RESPONSE`](#toc-063c)
        - [2.2.4.14.3. `FS_OPEN_RESPONSE`](#toc-063d)
        - [2.2.4.14.4. `FS_READ_RESPONSE`](#toc-063e)
        - [2.2.4.14.5. `FS_CLOSE_RESPONSE`](#toc-063f)
    - [2.2.5. Структура `SRV_EVENT_BODY`](#toc-064)
      - [2.2.5.1. Структура `NOTIFY_EVENT`](#toc-064a)
        - [2.2.5.1.1. Поле `time_us`](#toc-064b)
        - [2.2.5.1.2. Поле `ev_type`](#toc-064c)
        - [2.2.5.1.3. Поле `status`](#toc-064d)
        - [2.2.5.1.4. Поле `source`](#toc-064e)
        - [2.2.5.1.5. Поле `bus`](#toc-064f)
        - [2.2.5.1.6. Поле `op_kind`](#toc-064g)
        - [2.2.5.1.7. Поля `arg0/arg1/arg2`](#toc-064h)
      - [2.2.5.2. Структура `PL_STREAM_EVENT`](#toc-065)
        - [2.2.5.2.1. Поле `ev_flags`](#toc-066)
        - [2.2.5.2.2. Поле `lost_delta`](#toc-067)
        - [2.2.5.2.3. Поле `data_len`](#toc-068)
  - [2.3. Сервис FS: адресация, потоки и жизненный цикл](#fs-service)
    - [Адресация и ограничения](#fs-paths)
    - [Потоки FILE, TREE и TAR](#fs-streams)
    - [Дескрипторы, повторы и ошибки](#fs-lifecycle)
    - [Примеры обмена](#fs-examples)


<a id="toc-001"></a>
## Часть I. Концепция и принципы работы DCP2

<a id="toc-002"></a>
### 1. Назначение протокола

DCP2 — это прикладной протокол обмена между внешним клиентом и устройством по TCP. Его задача — дать единый, предсказуемый и удобный способ передавать команды, получать результаты и принимать асинхронные уведомления в рамках одного соединения.

Протокол спроектирован так, чтобы оставаться простым для реализации и расширяемым по мере развития системы. Общий формат сообщений сохраняет целостность взаимодействия между компонентами, а разделение обмена на сервисы помогает поддерживать понятную структуру и масштабируемость.

Основной принцип DCP2 — единые правила работы для всех сценариев обмена. Это снижает сложность клиентской и серверной логики, упрощает сопровождение и делает поведение протокола устойчивым при добавлении новых возможностей.

Документ ниже последовательно описывает структуру сообщений, назначение полей и форматы данных, используемые в рамках DCP2.

<a id="toc-003"></a>
### 2. Архитектура протокола

DCP2 работает как прикладной слой поверх установленного TCP-соединения между клиентом и устройством.
На уровне транспорта данные поступают как непрерывный байтовый поток, поэтому границы логических сообщений определяются средствами самого DCP2.

Каждый кадр DCP2 состоит из двух логических частей:

`DCP2_FRAME = DCP2_HDR + DCP2_PAYLOAD`

<a id="toc-004"></a>
#### 2.1. Заголовок фрейма `DCP2_HDR`

Загловок `DCP2_HDR` решает транспортно-прикладную задачу фрейминга сообщений в TCP-потоке.
Поскольку TCP не сохраняет границы сообщений, именно заголовок позволяет корректно определить начало кадра, проверить его базовую корректность и понять, где заканчивается текущий кадр.

На практике `DCP2_HDR` используется для:
синхронизации и выделения границ сообщения в непрерывном потоке, первичной валидации входного кадра до передачи в сервисную логику, контроля совместимости версии протокола и определения длины полезной части кадра для полного чтения `DCP2_PAYLOAD`.

`DCP2_HDR` отделяет задачу надёжного разбора потока от прикладной обработки команд.

<a id="toc-005"></a>
#### 2.2. Полезная нагрузка фрейма `DCP2_PAYLOAD`

`DCP2_PAYLOAD` описывает смысл сообщения после того, как кадр выделен и принят как валидный.
Эта часть отвечает за маршрутизацию в нужный сервис и за интерпретацию конкретной операции.

Логика обработки `DCP2_PAYLOAD` последовательна: `srv` определяет целевой сервис-обработчик, `message.op` задаёт тип сообщения (`request`, `response`, `event`) и код операции, `message.seq` обеспечивает корреляцию между запросами и ответами, а `message.body` разбирается по контексту `srv + тип + opcode`.

Такой подход даёт единый каркас обмена для всех сервисов: внешняя форма сообщения одинакова, а внутренняя структура тела выбирается контекстно и может расширяться без изменения базовой модели протокола.
Точные форматы полей и допустимые значения приводятся в части II (формальная спецификация).

<a id="toc-006"></a>
#### 2.3. Карта структур DCP2

```text
TCP_APP_DATA (byte stream)
└── DCP2_FRAME
    ├── DCP2_HDR
    │   ├── magic[4]
    │   ├── ver
    │   └── dcp_len
    └── DCP2_PAYLOAD
        ├── srv (дискриминатор сервиса)
        │   ├── 0x00 -> PING
        │   ├── 0x01 -> MEM
        │   ├── 0x02 -> I2C
        │   ├── 0x03 -> SMI
        │   ├── 0x04 -> SPI
        │   ├── 0x05 -> UART
        │   ├── 0x06 -> NOTIFY
        │   └── 0x07 -> FS
        └── SRV_MESSAGE (формат зависит от srv)
            ├── op   (RESP/EVENT/OPCODE)
            │   └── OPCODE (зависит от srv):
            │       ├── srv=PING (0x00)
            │       │   └── 0x00 PING
            │       │       ├── request  -> PING_REQUEST
            │       │       └── response -> PING_RESPONSE
            │       ├── srv=MEM (0x01)
            │       │   ├── 0x00 MEM_READ
            │       │   │   ├── request  -> MEM_READ_REQUEST
            │       │   │   └── response -> MEM_READ_RESPONSE
            │       │   └── 0x01 MEM_WRITE
            │       │       ├── request  -> MEM_WRITE_REQUEST
            │       │       └── response -> MEM_WRITE_RESPONSE
            │       ├── srv=I2C (0x02)
            │       │   ├── 0x00 I2C_READ_REG
            │       │   │   ├── request  -> I2C_READ_REG_REQUEST
            │       │   │   └── response -> I2C_READ_REG_RESPONSE
            │       │   ├── 0x01 I2C_WRITE_REG
            │       │   │   ├── request  -> I2C_WRITE_REG_REQUEST
            │       │   │   └── response -> I2C_WRITE_REG_RESPONSE
            │       │   └── 0x02 I2C_POLICY_SET
            │       │       ├── request  -> I2C_POLICY_SET_REQUEST
            │       │       └── response -> I2C_POLICY_SET_RESPONSE
            │       ├── srv=SMI (0x03)
            │       │   ├── 0x00 SMI_READ
            │       │   │   ├── request  -> SMI_READ_REQUEST
            │       │   │   └── response -> SMI_READ_RESPONSE
            │       │   └── 0x01 SMI_WRITE
            │       │       ├── request  -> SMI_WRITE_REQUEST
            │       │       └── response -> SMI_WRITE_RESPONSE
            │       ├── srv=NOTIFY (0x06)
            │       │   ├── 0x10 SUBSCRIBE
            │       │   │   ├── request  -> NOTIFY_SUBSCRIBE_REQUEST
            │       │   │   ├── response -> NOTIFY_SUBSCRIBE_RESPONSE
            │       │   │   └── event    -> NOTIFY_EVENT
            │       │   └── 0x11 UNSUBSCRIBE
            │       │       ├── request  -> NOTIFY_UNSUBSCRIBE_REQUEST
            │       │       └── response -> NOTIFY_UNSUBSCRIBE_RESPONSE
            │       ├── srv=FS (0x07)
            │       │   ├── 0x00 FS_INFO
            │       │   │   ├── request  -> FS_INFO_REQUEST
            │       │   │   └── response -> FS_INFO_RESPONSE
            │       │   ├── 0x01 FS_STAT
            │       │   │   ├── request  -> FS_STAT_REQUEST
            │       │   │   └── response -> FS_STAT_RESPONSE
            │       │   ├── 0x02 FS_OPEN
            │       │   │   ├── request  -> FS_OPEN_REQUEST
            │       │   │   └── response -> FS_OPEN_RESPONSE
            │       │   ├── 0x03 FS_READ
            │       │   │   ├── request  -> FS_READ_REQUEST
            │       │   │   └── response -> FS_READ_RESPONSE
            │       │   └── 0x04 FS_CLOSE
            │       │       ├── request  -> FS_CLOSE_REQUEST
            │       │       └── response -> FS_CLOSE_RESPONSE
            │       └── srv=PL (I2C/SMI/SPI/UART)
            │           ├── 0x10 SUBSCRIBE_STREAM
            │           │   ├── request  -> PL_SUBSCRIBE_STREAM_REQUEST
            │           │   ├── response -> PL_SUBSCRIBE_STREAM_RESPONSE
            │           │   └── event    -> PL_STREAM_EVENT
            │           └── 0x11 UNSUBSCRIBE_STREAM
            │               ├── request  -> PL_UNSUBSCRIBE_STREAM_REQUEST
            │               └── response -> PL_UNSUBSCRIBE_STREAM_RESPONSE
            ├── seq
            └── body (определяется по RESP/EVENT и srv/opcode)
                ├── request  -> SRV_REQUEST_BODY
                ├── response -> SRV_RESPONSE_BODY
                └── event    -> SRV_EVENT_BODY
```


<a id="toc-007"></a>
### 3. Модель обмена
<a id="toc-008"></a>
#### 3.1. Участники и роли

Обмен строится между двумя сторонами: внешним клиентом и сервером на устройстве. Клиент инициирует управляющие операции и ожидает результат, сервер принимает сообщения, маршрутизирует их по сервисам и возвращает ответы. Асинхронные события инициируются сервером.

<a id="toc-009"></a>
#### 3.2. Типы сообщений

В протоколе используются три типа сообщений:

| Тип | Инициатор | Назначение |
|---|---|---|
| `request` | Клиент | Запрос на выполнение операции |
| `response` | Сервер | Результат обработки запроса |
| `event` | Сервер | Асинхронное сообщение без отдельного запроса в момент отправки |

```text
Клиент                          Сервер
  |                               |
  |-------- request ------------->|
  |<------- response -------------|
  |                               |
  |<======== event ===============|   (асинхронно, по инициативе сервера)
```

`request` используется для инициирования действия (чтение-запись, изменение политики, управление подпиской), `response` является обязательным итогом обработки `request`, а `event` передаётся сервером по собственной инициативе при появлении нового состояния или данных (например, в активной подписке на стрим).

<a id="toc-010"></a>
#### 3.3. Корреляция сообщений

`seq` — это идентификатор логической операции в рамках одного TCP-соединения.
Он нужен для того, чтобы клиент мог однозначно сопоставить полученный `response` с конкретным ранее отправленным `request`, даже если в канале одновременно выполняется несколько операций.

Правила использования `seq`:

| Контекст   | Требование                                                          |
| ---------- | ------------------------------------------------------------------- |
| `request`  | Значение `seq` назначает клиент в диапазоне `1..0xFFFF`             |
| `response` | Сервер возвращает то же `seq`, что было в соответствующем `request` |
| `event`    | Используется `seq = 0`                                              |

Рекомендуемая практика на стороне клиента: использовать монотонно возрастающий счётчик `seq` с переходом через `0xFFFF` по кругу, не переиспользовать номер до получения итогового `response`, вести таблицу активных запросов по ключу `seq` и не применять `seq = 0` для обычных запросов.

Пример корреляции при параллельных запросах:

Клиент отправляет три запроса: `MEM_READ(seq=0x0101)`, `I2C_READ_REG(seq=0x0102)`, `SMI_READ(seq=0x0103)`. Сервер может вернуть ответы в другом порядке, например: `response(seq=0x0103)`, затем `response(seq=0x0101)`, затем `response(seq=0x0102)`. Клиент сопоставляет каждый ответ по `seq` с таблицей активных запросов, а не по порядку получения, после чего соответствующий запрос помечается завершённым независимо от позиции ответа в потоке.

<a id="toc-011"></a>
#### 3.4. Модель ошибок

Результат операции интерпретируется в два этапа: сначала анализируется общий `status`, затем (только при успешном выполнении) разбирается тело ответа. Полный перечень кодов и их формальные значения приведён в разделе `2.2.4.1. Поле status`; в данном разделе фиксируется практическое поведение сторон протокола.

Сервер при ошибке возвращает `response` с соответствующим кодом `status` и завершает обработку текущего запроса. Ошибка не должна нарушать обработку других независимых запросов в том же соединении.

Клиент при получении ошибки считает текущую операцию завершённой с отказом, выполняет действие по классу ошибки и продолжает работу соединения, если транспорт остаётся валидным.

| Класс ошибки | Типичные коды | Действие клиента |
|---|---|---|
| Ошибка формата или протокола | `ERR_MALFORMED`, `ERR_UNSUPPORTED` | Считать запрос некорректным, не отправлять повторно в том же виде, исправить параметры |
| Временная ошибка ресурса | `ERR_BUSY`, `ERR_TIMEOUT` | Повторить запрос после задержки с ограничением числа попыток |
| Ошибка доступа или политики | `ERR_DENIED` | Не повторять запрос до изменения политики, прав или режима работы |
| Ошибка параметров запроса | `ERR_RANGE` | Скорректировать параметры и отправить новый корректный запрос |
| Внутренняя ошибка сервиса | `ERR_INTERNAL` | Допустить ограниченное число повторных попыток, затем зафиксировать ошибку и уведомить оператора/журнал |

<a id="toc-012"></a>
#### 3.5. Типовые сценарии обмена

Ниже приведены базовые сценарии, по которым разбирается и обрабатывается входящий фрейм `DCP2`.

<a id="toc-013"></a>
##### 3.5.1. Корректный `request` с успешным ответом

Пример: `srv=MEM`, `op=request/MEM_READ`, `seq=0x0101`.

Кадр, обрабатываемый сервером (hex):

```text
44 43 50 32 | 00 02 | 00 0C | 01 | 00 | 01 01 | 00 20 40 00 00 00 00 02
magic       | ver   | dcp_len|srv| op | seq   | MEM_READ_REQUEST
```

Семантика полей:

| Поле                     | Значение                                    |
| ------------------------ | ------------------------------------------- |
| `DCP2_HDR.magic`         | `44 43 50 32` (`"DCP2"`)                    |
| `DCP2_HDR.ver`           | `00 02`                                     |
| `DCP2_HDR.dcp_len`       | `00 0C`                                     |
| `DCP2_PAYLOAD.srv`       | `01` (`MEM`)                                |
| `SRV_MESSAGE.op`         | `00` (`request`, `OPCODE=0x00`, `MEM_READ`) |
| `SRV_MESSAGE.seq`        | `01 01` (`0x0101`)                          |
| `MEM_READ_REQUEST.flags` | `00`                                        |
| `MEM_READ_REQUEST.width` | `20` (`32` бита)                            |
| `MEM_READ_REQUEST.addr`  | `40 00 00 00`                               |
| `MEM_READ_REQUEST.count` | `00 02`                                     |

Кадр, обрабатываемый клиентом (hex):

```text
44 43 50 32 | 00 02 | 00 0A | 01 | 80 | 01 01 | 00 00 | DE AD BE EF
magic       | ver   | dcp_len|srv| op | seq   |status | data
```

Семантика полей:

| Поле                     | Значение                                     |
| ------------------------ | -------------------------------------------- |
| `DCP2_HDR.magic`         | `44 43 50 32` (`"DCP2"`)                     |
| `DCP2_HDR.ver`           | `00 02`                                      |
| `DCP2_HDR.dcp_len`       | `00 0A`                                      |
| `DCP2_PAYLOAD.srv`       | `01` (`MEM`)                                 |
| `SRV_MESSAGE.op`         | `80` (`response`, `OPCODE=0x00`, `MEM_READ`) |
| `SRV_MESSAGE.seq`        | `01 01` (`0x0101`)                           |
| `SRV_RESPONSE.status`    | `00 00` (`OK`)                               |
| `MEM_READ_RESPONSE.data` | `DE AD BE EF`                                |

<a id="toc-014"></a>
##### 3.5.2. Неподдерживаемый сервис или операция

Пример: `srv` не зарегистрирован или `opcode` не поддерживается для выбранного `srv`.

Кадр, обрабатываемый сервером (hex):

```text
44 43 50 32 | 00 02 | 00 04 | 02 | 3F | 01 02
magic       | ver   | dcp_len|srv| op | seq
```

Семантика полей:

| Поле               | Значение                                                    |
| ------------------ | ----------------------------------------------------------- |
| `DCP2_HDR.magic`   | `44 43 50 32` (`"DCP2"`)                                    |
| `DCP2_HDR.ver`     | `00 02`                                                     |
| `DCP2_HDR.dcp_len` | `00 04`                                                     |
| `DCP2_PAYLOAD.srv` | `02` (`I2C`)                                                |
| `SRV_MESSAGE.op`   | `3F` (`request`, `OPCODE=0x3F`, операция не поддерживается) |
| `SRV_MESSAGE.seq`  | `01 02` (`0x0102`)                                          |

Кадр, обрабатываемый клиентом (hex):

```text
44 43 50 32 | 00 02 | 00 06 | 02 | BF | 01 02 | 00 02
magic       | ver   | dcp_len|srv| op | seq   | status
```

Семантика полей:

| Поле                  | Значение                         |
| --------------------- | -------------------------------- |
| `DCP2_HDR.magic`      | `44 43 50 32` (`"DCP2"`)         |
| `DCP2_HDR.ver`        | `00 02`                          |
| `DCP2_HDR.dcp_len`    | `00 06`                          |
| `DCP2_PAYLOAD.srv`    | `02` (`I2C`)                     |
| `SRV_MESSAGE.op`      | `BF` (`response`, `OPCODE=0x3F`) |
| `SRV_MESSAGE.seq`     | `01 02` (`0x0102`)               |
| `SRV_RESPONSE.status` | `00 02` (`ERR_UNSUPPORTED`)      |

<a id="toc-015"></a>
##### 3.5.3. Ошибка параметров запроса

Пример: несогласованная длина `body`, недопустимые флаги, выход параметров за диапазон.

Кадр, обрабатываемый сервером (hex):

```text
44 43 50 32 | 00 02 | 00 0E | 01 | 01 | 01 03 | 00 20 40 00 00 00 00 02 AA BB
magic       | ver   | dcp_len|srv| op | seq   | MEM_WRITE_REQUEST (invalid data len)
```

Семантика полей:

| Поле                      | Значение                                               |
| ------------------------- | ------------------------------------------------------ |
| `DCP2_HDR.magic`          | `44 43 50 32` (`"DCP2"`)                               |
| `DCP2_HDR.ver`            | `00 02`                                                |
| `DCP2_HDR.dcp_len`        | `00 0E`                                                |
| `DCP2_PAYLOAD.srv`        | `01` (`MEM`)                                           |
| `SRV_MESSAGE.op`          | `01` (`request`, `OPCODE=0x01`, `MEM_WRITE`)           |
| `SRV_MESSAGE.seq`         | `01 03` (`0x0103`)                                     |
| `MEM_WRITE_REQUEST.flags` | `00`                                                   |
| `MEM_WRITE_REQUEST.width` | `20` (`32` бита)                                       |
| `MEM_WRITE_REQUEST.addr`  | `40 00 00 00`                                          |
| `MEM_WRITE_REQUEST.count` | `00 02`                                                |
| `MEM_WRITE_REQUEST.data`  | `AA BB` (некорректная длина для `count=2`, `width=32`) |

Кадр, обрабатываемый клиентом (hex):

```text
44 43 50 32 | 00 02 | 00 06 | 01 | 81 | 01 03 | 00 01
magic       | ver   | dcp_len|srv| op | seq   | status
```

Семантика полей:

| Поле                  | Значение                                      |
| --------------------- | --------------------------------------------- |
| `DCP2_HDR.magic`      | `44 43 50 32` (`"DCP2"`)                      |
| `DCP2_HDR.ver`        | `00 02`                                       |
| `DCP2_HDR.dcp_len`    | `00 06`                                       |
| `DCP2_PAYLOAD.srv`    | `01` (`MEM`)                                  |
| `SRV_MESSAGE.op`      | `81` (`response`, `OPCODE=0x01`, `MEM_WRITE`) |
| `SRV_MESSAGE.seq`     | `01 03` (`0x0103`)                            |
| `SRV_RESPONSE.status` | `00 01` (`ERR_MALFORMED`)                     |

<a id="toc-016"></a>
##### 3.5.4. Обработка входящего `event`

Пример: `op=event/...`, `seq=0x0000`.

Кадр, обрабатываемый клиентом (hex):

```text
44 43 50 32 | 00 02 | 00 0F | 02 | 50 | 00 00
magic       | ver   | dcp_len|srv| op | seq

00 | 00 00 00 00 | 00 04 | 11 22 33 44
evf| lost_delta  | data_len | data
```

Семантика полей:

| Поле                         | Значение                                        |
| ---------------------------- | ----------------------------------------------- |
| `DCP2_HDR.magic`             | `44 43 50 32` (`"DCP2"`)                        |
| `DCP2_HDR.ver`               | `00 02`                                         |
| `DCP2_HDR.dcp_len`           | `00 0F`                                         |
| `DCP2_PAYLOAD.srv`           | `02` (`I2C`)                                    |
| `SRV_MESSAGE.op`             | `50` (`event`, `OPCODE=0x10`, `STREAM`)         |
| `SRV_MESSAGE.seq`            | `00 00` (`0x0000`, зарезервировано для событий) |
| `PL_STREAM_EVENT.ev_flags`   | `00`                                            |
| `PL_STREAM_EVENT.lost_delta` | `00 00 00 00`                                   |
| `PL_STREAM_EVENT.data_len`   | `00 04`                                         |
| `PL_STREAM_EVENT.data`       | `11 22 33 44`                                   |


<a id="toc-017"></a>
### 4. Расширяемость и совместимость

`DCP2` ориентирован на эволюцию без изменения базового каркаса обмена: новые возможности добавляются через новые сервисы и операции, а действующие сценарии сохраняют предсказуемое поведение.

На концептуальном уровне целевое правило простое: расширение протокола не должно ломать ранее реализованных клиентов, если они не используют новые возможности.

<a id="toc-018"></a>
### 5. Нормативные правила интерпретации

<a id="toc-019"></a>
#### 5.1. Порядок байтов

Все поля размером более 1 байта (`u16`, `u32`, `u64`) передаются в сетевом порядке байтов (big-endian). Это правило едино для заголовка, полезной нагрузки, кодов состояния и полей событий.

<a id="toc-020"></a>
#### 5.2. Поведение при ошибках фрейминга

Если входящий кадр не проходит базовую проверку (`magic`, `ver`, `dcp_len`), сервер отклоняет кадр как некорректный. Поведение соединения после отклонения определяется реализацией: допускается продолжение приёма следующего кадра или закрытие соединения при устойчивой десинхронизации потока.

<a id="toc-021"></a>
#### 5.3. Глобальные ограничения длины

Реализация обязана проверять длины всех переменных полей до начала выполнения операции. При несоответствии длины фактическому формату сообщения должен возвращаться `ERR_MALFORMED`, а при превышении допустимых значений параметров — `ERR_RANGE`.

Текущая реализация `bvstk` принимает `4 <= dcp_len <= 4096`. Лимит относится ко
всему payload, включая `srv`, `op`, `seq` и `status` ответа. Правила порционной
передачи файловых результатов приведены в [описании FS](#fs-service).

Пример: клиент отправил `MEM_WRITE` с `count=2`, `width=32`, но передал только 2 байта `data` вместо ожидаемых 8. Такая длина тела не соответствует формату `MEM_WRITE_REQUEST`, поэтому сервер должен отклонить запрос и вернуть `response` со статусом `ERR_MALFORMED`.

<a id="toc-022"></a>
#### 5.4. Правила использования `seq`

`seq` используется как ключ корреляции `request`/`response` в пределах одного TCP-соединения и применяется по следующим правилам:

1. Клиент назначает `seq` в диапазоне `0x0001..0xFFFF`; значение `0x0000` для `request` не используется (зарезервировано для `event`).
2. Клиент обязан вести таблицу активных запросов (ключ — `seq`) для корреляции `response`, контроля таймаутов и безопасного переиспользования номеров.
3. Новый `request` может получить только свободный `seq`, которого нет в таблице активных запросов.
4. `seq` считается освобождённым после перехода операции в терминальное состояние: получен валидный `response` с тем же `seq` или запрос закрыт локально по таймауту/отмене.
5. При переполнении счётчика клиент продолжает выдачу по кругу (`...0xFFFE`, `0xFFFF`, `0x0001`), пропуская занятые значения.
6. `response` с неизвестным или устаревшим `seq` не должен нарушать обработку активных запросов; такие сообщения обрабатываются по политике клиента (например, логирование и игнорирование).

<a id="toc-023"></a>
#### 5.5. Таймауты и повторные попытки

Клиент применяет таймаут к каждому активному `request` и ведёт счётчик попыток.

Базовые параметры по умолчанию:

| Параметр | Значение |
|---|---|
| `T_resp` (ожидание `response`) | `1000 ms` |
| `N_retry_max` | `2` повторные попытки (итого до 3 отправок) |

`T_resp` применяется одинаково для всех типов `request` в рамках текущей версии протокола.

Правила обработки:

1. При отправке `request(seq=N)` клиент создаёт запись в таблице активных запросов: `seq`, тип операции, время отправки, дедлайн, номер попытки.
2. Если до дедлайна получен валидный `response(seq=N)`, запрос завершается успешно или ошибкой по `status`, `seq` освобождается.
3. Если дедлайн истёк, запрос закрывается локально с признаком timeout, текущий `seq` освобождается.
4. Повторная отправка выполняется только как новый `request` с новым `seq`; повторно использовать старый `seq` для retry недопустимо.
5. Повторные попытки выполняются только для временных условий (`ERR_BUSY`, `ERR_TIMEOUT`, отсутствие ответа). Для `ERR_MALFORMED`, `ERR_RANGE`, `ERR_DENIED`, `ERR_UNSUPPORTED` повтор без изменения параметров не выполняется.
6. После достижения `N_retry_max` операция завершается ошибкой timeout на стороне клиента.
7. Поздний `response` по уже закрытому запросу (устаревший `seq`) не влияет на состояние активных операций: такой кадр логируется и игнорируется.

<a id="toc-023a"></a>
#### 5.6. Обнаружение возможностей сервера

В текущей версии протокола отдельная операция `CAPABILITIES` не используется. Клиент ориентируется на фиксированный контракт версии протокола и проверяет поддержку операций по факту ответа сервера.

Если операция или сервис не поддерживаются, сервер возвращает `ERR_UNSUPPORTED`, после чего клиент выбирает совместимый сценарий или отключает соответствующую функцию.

Операция `CAPABILITIES` может быть добавлена в будущих версиях как опциональное расширение, если потребуется динамическое согласование возможностей.

Необязательный сервис `FS` (`srv=0x07`) обнаруживается запросом `FS_INFO`.
Его ответ описывает только возможности файлового сервиса, включая версию его
контракта и доступные тома. Отсутствие сервиса даёт `ERR_UNSUPPORTED` и не влияет
на остальные операции DCP2. Глобальная операция `CAPABILITIES` не требуется.

<a id="toc-023b"></a>
#### 5.7. Политика версионирования

Поле `ver` в `DCP2_HDR` определяет версию базового wire-контракта протокола.

Таблица версий:

| `ver` | Статус | Содержание |
|---|---|---|
| `0x0001` | legacy baseline | Базовый каркас DCP2: `PING`, `MEM`, `I2C`, `SMI`, `SPI`, `UART` |
| `0x0002` | current | Добавлена формальная политика версионирования и сервис `NOTIFY` для асинхронных control/state уведомлений |

Нормативные правила:

1. Сервер обязан проверять `ver` до обработки `DCP2_PAYLOAD` и отклонять неподдерживаемую версию кадра.
2. Клиент обязан трактовать `ver` как основной признак совместимости wire-формата.
3. Изменения документации без изменения бинарного формата и семантики кадров не меняют `ver`.
4. Обратно-совместимые расширения могут сохранять `ver`, только если не меняют формат и семантику уже существующих `srv`, `OPCODE`, `status` и обязательных полей.
5. Изменения, затрагивающие формат `DCP2_HDR`, существующие `body`-структуры, семантику существующих полей или вводящие новый обязательный для корректной совместимости сервис, обязаны повышать `ver`.
6. Для версии `0x0002` сервис `NOTIFY` считается частью формального контракта протокола.

FS v1 — необязательное расширение `ver=0x0002`: заголовок, существующие сервисы,
коды операций и значения статусов сохраняют свою семантику. `FS_INFO.fs_version`
равен `1`; поддержку файловых режимов клиент определяет по его `capabilities`.

---

<a id="toc-024"></a>
## Часть II. Формальная спецификация DCP2

<a id="toc-025"></a>
## 1. Структура DCP2_HDR

```c
typedef struct {
    u8  magic[4];
    u16 ver;
    u16 dcp_len;
} DCP2_HDR;

```

**Размер**: 8 байт
**Назначение**: Предназначен для фрейминга, синхронизации и контроля версии сообщений DCP2 в потоковом транспорте TCP

<a id="toc-026"></a>
### 1.1. Поле `magic`

**Размер**: 4 байта
**Назначение**: Сигнатура протокола. Имеет фиксированное значение в ASCII "DCP2" или в шестнадцатеричном виде `0x44 0x43 0x50 0x32`
**Пример**: `44 43 50 32`

<a id="toc-027"></a>
### 1.2. Поле `ver`

**Размер**: 2 байта
**Назначение**: Версия wire-контракта протокола DCP2.
**Допустимые значения**:
- `0x0001` — legacy baseline
- `0x0002` — текущая версия документа
**Пример**: `00 02`

<a id="toc-028"></a>
### 1.3. Поле `dcp_len`

**Размер**: 2 байта
**Назначение**: Длина поля `DCP2_PAYLOAD` в байтах
**Пример**: `00 04`

---

<a id="toc-029"></a>
## 2. Структура `DCP2_PAYLOAD`

```c
typedef struct {
    u8          srv;
    SRV_MESSAGE message;
} DCP2_PAYLOAD;
```

**Размер**: `DCP_LEN` байт
**Назначение**:  Предназначена для инкапсуляции одного логического сообщения DCP2, мультиплексирования сервисов внутри протокола и передачи данных конкретной подсистеме (PING, MEM, I2C и т.д.)

<a id="toc-030"></a>
### 2.1. Поле `srv`

**Размер**: 1 байт
**Назначение**:  Идентификатор сервиса (дискриминатор), определяющий формат и семантику `service_message`.

Таблица допустимых значений:

|Значение|Сервис|
|---|---|
|0x00|PING|
|0x01|MEM|
|0x02|I2C|
|0x03|SMI|
|0x04|SPI|
|0x05|UART|
|0x06|NOTIFY|
|0x07|FS — файлы, каталоги и рекурсивный TAR; необязательный сервис|

<a id="toc-031"></a>
### 2.2. Структура `SRV_MESSAGE`

```c
typedef struct {
    u8  op;
    u16 seq;

    /* тип body определяется битами RESP/EVENT в op */

    union {
        SRV_REQUEST_BODY  request;
        SRV_RESPONSE_BODY response;
        SRV_EVENT_BODY    event;
    } body;

} SRV_MESSAGE;

```

**Размер**: `DCP_LEN - 1` байт (в составе `DCP2_PAYLOAD`)
**Назначение**:  Содержит одно логическое сообщение выбранного сервиса (запрос, ответ или асинхронное событие).
**Примечание**:
1. Все сообщения сервиса начинаются с общего заголовка (`op`, `seq`), обеспечивающего:
	- унифицированную корреляцию request/response
	- поддержку асинхронных событий
	- единый механизм обработки ошибок
2. Формат тела сообщения определяется значением `srv`, типом сообщения (битами `RESP`/`EVENT`) и `OPCODE` (bit5..0 поля `op`).
3.  `union` приведён только для пояснения формата.  В реальном потоке данных после поля `seq` сразу идут поля сообщения, длина которых зависит от типа сообщения.

<a id="toc-032"></a>
#### 2.2.1. Поле `op`

**Размер**: 1 байт
**Назначение**:  Код операции внутри сервиса и тип сообщения.

Битовое представление:
- `bit7` — `RESP` (0=request, 1=response)
- `bit6` — `EVENT` (0=обычное сообщение, 1=асинхронное событие)
- `bit5..0` — `OPCODE` (0..63)

<a id="toc-033"></a>
#### 2.2.2. Поле `seq`

**Размер**: 2 байта
**Назначение**:  Идентификатор сообщения для корреляции request/response.
- В запросе задаётся клиентом в диапазоне `1..0xFFFF`.
- В ответе сервер повторяет значение `seq`.
- Значение `seq = 0` зарезервировано для асинхронных событий (`event`).

<a id="toc-034"></a>
#### 2.2.3. Структура `SRV_REQUEST_BODY`

```c
typedef struct {
    union {
        PING_REQUEST ping;

        MEM_READ_REQUEST  mem_read;
        MEM_WRITE_REQUEST mem_write;

        I2C_READ_REG_REQUEST    i2c_read_reg;
        I2C_WRITE_REG_REQUEST   i2c_write_reg;
        I2C_POLICY_SET_REQUEST  i2c_policy_set;

        SMI_READ_REQUEST  smi_read;
        SMI_WRITE_REQUEST smi_write;

        NOTIFY_SUBSCRIBE_REQUEST   notify_subscribe;
        NOTIFY_UNSUBSCRIBE_REQUEST notify_unsubscribe;

        FS_INFO_REQUEST  fs_info;
        FS_STAT_REQUEST  fs_stat;
        FS_OPEN_REQUEST  fs_open;
        FS_READ_REQUEST  fs_read;
        FS_CLOSE_REQUEST fs_close;

        PL_SUBSCRIBE_STREAM_REQUEST   pl_subscribe_stream;
        PL_UNSUBSCRIBE_STREAM_REQUEST pl_unsubscribe_stream;

    } u;
} SRV_REQUEST_BODY;
```

**Размер**: переменный; определяется значениями `srv` и `OPCODE` (bit5..0 поля `op`)
**Назначение**:  Содержит тело запроса (request) для выбранного сервиса и операции

<a id="toc-035"></a>
##### 2.2.3.1 Структура `PING_REQUEST`

```c
typedef struct {
    /* пусто */
} PING_REQUEST;
```

**Размер**: 0 байт
**Назначение**:  Используется для проверки доступности соединения и базовой работоспособности сервера.
**Примечание**: Структура `PING_REQUEST` не содержит дополнительных полей. Запрос состоит только из общего заголовка `SRV_MESSAGE`

<a id="toc-036"></a>
##### 2.2.3.2 Структура `MEM_READ_REQUEST`

```c
typedef struct {
    u8  flags;
    u8  width;
    u32 addr;
    u16 count;
} MEM_READ_REQUEST;
```

**Размер**: 8 байт
**Назначение**:  Определяет параметры операции чтения памяти (MMIO/AXI адресного пространства).
**Примечание**: Контекст использования — `srv = 0x01` (`MEM`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x00`.

<a id="toc-037"></a>
###### 2.2.3.2.1. Поле `flags`

**Размер**: 1 байт
**Назначение**:  Определяет режим выполнения операции чтения.

Битовое представление:
- `bit0` — `AUTOINC`
    - `0` — чтение всех элементов из одного и того же адреса
    - `1` — после каждого чтения адрес увеличивается на `width / 8`
- `bit7..1` — зарезервированы (должны быть равны 0)

**Примечание**: Если зарезервированные биты установлены → `ERR_MALFORMED`.

<a id="toc-038"></a>
###### 2.2.3.2.2. Поле `width`

**Размер**: 1 байт
**Назначение**: Определяет разрядность одного читаемого элемента в битах.

Допустимые значения:
- `8`
- `16`
- `32`
- `64`

**Примечание**:
- Если значение не поддерживается реализацией → `ERR_UNSUPPORTED`.
- При `AUTOINC = 1` шаг увеличения адреса равен `width / 8` байт.

<a id="toc-039"></a>
###### 2.2.3.2.3. Поле `addr`

**Размер**: 4 байта
**Назначение**: Базовый адрес чтения в адресном пространстве памяти (MMIO/AXI).

**Примечание**:
- Интерпретация адреса определяется реализацией.
- Если адрес выходит за разрешённый диапазон → `ERR_RANGE`.

<a id="toc-040"></a>
##### 2.2.3.3. Структура `MEM_WRITE_REQUEST`

```c
typedef struct {
    u8  flags;
    u8  width;
    u32 addr;
    u16 count;
    u8  data[];   /* длина = count * (width / 8) */
} MEM_WRITE_REQUEST;

```

**Размер**: `8 + count * (width / 8)` байт
**Назначение**:  Определяет параметры операции записи в память (MMIO/AXI адресное пространство).
**Примечание**: Контекст использования — `srv = 0x01` (`MEM`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x01`.

<a id="toc-041"></a>
##### 2.2.3.4. Структура `I2C_READ_REG_REQUEST`
```c
typedef struct {
    u8 addr7;
    u8 reg;
} I2C_READ_REG_REQUEST;

```

**Размер**: 2 байта
**Назначение**:  Определяет параметры операции чтения одного регистра I2C-устройства.
**Примечание**: Контекст использования — `srv = 0x02` (`I2C`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x00`.

<a id="toc-042"></a>
##### 2.2.3.5. Структура `I2C_WRITE_REG_REQUEST`

```c
typedef struct {
    u8 addr7;
    u8 reg;
    u8 val;
} I2C_WRITE_REG_REQUEST;
```

**Размер**: 3 байта
**Назначение**: Определяет параметры операции записи одного регистра I2C-устройства.
**Примечание**: Контекст использования — `srv = 0x02` (`I2C`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x01`.

<a id="toc-043"></a>
##### 2.2.3.6. Структура `I2C_POLICY_SET_REQUEST`

```c
typedef struct {
    u8 addr7;
    u8 policy;
} I2C_POLICY_SET_REQUEST;
```

**Размер**: 2 байта
**Назначение**: Изменяет политику доступа к операциям записи для целевого I2C-устройства.
**Примечание**: Контекст использования — `srv = 0x02` (`I2C`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x02`.

<a id="toc-044"></a>
###### 2.2.3.6.1. Поле `policy`

**Размер**: 1 байт
**Назначение**: Определяет политику выполнения операций записи.

Допустимые значения:
- `0x00` — `WHITELIST` (запись разрешена)
- `0x01` — `BLACKLIST` (запись запрещена)

**Примечание**: Любое другое значение поля `policy` должно приводить к `ERR_UNSUPPORTED`.

<a id="toc-045"></a>
##### 2.2.3.7. Структура `SMI_READ_REQUEST`

```c
typedef struct {
    u8 phy;
    u8 reg;
} SMI_READ_REQUEST;
```

**Размер**: 2 байта
**Назначение**: Определяет параметры операции чтения MDIO-регистра PHY.
**Примечание**: Контекст использования — `srv = 0x03` (`SMI`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x00`.

<a id="toc-046"></a>
##### 2.2.3.8. Структура `SMI_WRITE_REQUEST`

```c
typedef struct {
    u8  phy;
    u8  reg;
    u16 val16;
} SMI_WRITE_REQUEST;
```

**Размер**: 4 байта
**Назначение**: Определяет параметры операции записи MDIO-регистра PHY.
**Примечание**: Контекст использования — `srv = 0x03` (`SMI`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x01`.

<a id="toc-046a"></a>
##### 2.2.3.9. Структура `NOTIFY_SUBSCRIBE_REQUEST`

```c
typedef struct {
    u32 class_mask;
    u32 source_mask;
    u32 bus_mask;
    u8  flags;
} NOTIFY_SUBSCRIBE_REQUEST;
```

**Размер**: 13 байт
**Назначение**: Включает асинхронную доставку control/state событий для выбранных классов, источников и шин.
**Примечание**: Контекст использования — `srv = 0x06` (`NOTIFY`), `RESP = 0`, `EVENT = 0`, `OPCODE = 0x10`.

<a id="toc-046b"></a>
###### 2.2.3.9.1. Поле `class_mask`

**Размер**: 4 байта
**Назначение**: Маска классов событий, которые клиент хочет получать.

Битовое представление:
- `bit0` — `REG_ATTEMPT`
- `bit1` — `REG_COMMIT`
- `bit2` — `REG_DENIED`
- `bit3` — `STATE_CHANGED`
- `bit4` — `FAULT`
- `bit31..5` — зарезервированы

**Примечание**: Нулевая маска недопустима и должна приводить к `ERR_RANGE`.

<a id="toc-046c"></a>
###### 2.2.3.9.2. Поле `source_mask`

**Размер**: 4 байта
**Назначение**: Маска источников, от которых клиент хочет получать события.

Битовое представление:
- `bit0` — `TELNET`
- `bit1` — `HOST`
- `bit2` — `DCP`
- `bit3` — `INTERNAL`
- `bit31..4` — зарезервированы

**Примечание**: Нулевая маска недопустима и должна приводить к `ERR_RANGE`.

<a id="toc-046d"></a>
###### 2.2.3.9.3. Поле `bus_mask`

**Размер**: 4 байта
**Назначение**: Маска подсистем/шин, для которых клиент хочет получать уведомления.

Битовое представление:
- `bit0` — `I2C`
- `bit1` — `SMI`
- `bit2` — `SPI`
- `bit3` — `UART`
- `bit4` — `SYS`
- `bit31..5` — зарезервированы

**Примечание**: Нулевая маска недопустима и должна приводить к `ERR_RANGE`.

<a id="toc-046e"></a>
###### 2.2.3.9.4. Поле `flags`

**Размер**: 1 байт
**Назначение**: Дополнительные параметры подписки.

Битовое представление:
- `bit0` — `WITH_TIMESTAMP` (`1` — включать `time_us` в `NOTIFY_EVENT`)
- `bit1` — `SNAPSHOT_ON_SUBSCRIBE` (`1` — сервер может немедленно отправить служебное событие о текущем состоянии подписки)
- `bit7..2` — зарезервированы

**Примечание**: Если зарезервированные биты установлены → `ERR_MALFORMED`.

<a id="toc-046f"></a>
##### 2.2.3.10. Структура `NOTIFY_UNSUBSCRIBE_REQUEST`

```c
typedef struct {
    /* пусто */
} NOTIFY_UNSUBSCRIBE_REQUEST;
```

**Размер**: 0 байт
**Назначение**: Отключает асинхронную доставку событий сервиса `NOTIFY`.
**Примечание**: Запрос состоит только из общего заголовка `SRV_MESSAGE`.

<a id="toc-047"></a>
##### 2.2.3.11. Структура `PL_SUBSCRIBE_STREAM_REQUEST`

```c
typedef struct {
    u8 flags;
} PL_SUBSCRIBE_STREAM_REQUEST;
```

**Размер**: 1 байт
**Назначение**: Включает асинхронную передачу событий стрима для выбранного PL-сервиса (`I2C`, `SMI`, `SPI`, `UART`).
**Примечание**: Контекст использования — `srv = 0x02, 0x03, 0x04, 0x05`, `RESP = 0`, `EVENT = 0`, `OPCODE = 0x10`.

<a id="toc-048"></a>
###### 2.2.3.11.1. Поле `flags`

**Размер**: 1 байт
**Назначение**: Определяет дополнительные параметры подписки на стрим.

Битовое представление:
- `bit0` — `RAW_WORDS` (`1` — `DATA` в событиях кратно 4 байтам и трактуется как последовательность 32-битных слов)
- `bit1` — `WITH_TIMESTAMP` (`1` — в событие добавляется поле `time_us`)
- `bit2` — `RESET_LOST_COUNTERS` (`1` — сброс счётчиков потерь при успешной подписке)
- `bit7..3` — зарезервированы (должны быть равны 0)

**Примечание**: Если зарезервированные биты установлены → `ERR_MALFORMED`.

<a id="toc-049"></a>
##### 2.2.3.12. Структура `PL_UNSUBSCRIBE_STREAM_REQUEST`

```c
typedef struct {
    /* пусто */
} PL_UNSUBSCRIBE_STREAM_REQUEST;
```

**Размер**: 0 байт
**Назначение**: Отключает асинхронную передачу событий стрима для выбранного PL-сервиса.
**Примечание:** Запрос состоит только из общего заголовка `SRV_MESSAGE`.

<a id="toc-050a"></a>
##### 2.2.3.13. Структуры FS-запросов

Все перечисленные структуры используются при `srv = 0x07` (`FS`), `RESP = 0`
и `EVENT = 0`. Числовые поля передаются в big-endian, неявного выравнивания
между полями нет.

<a id="toc-050b"></a>
###### 2.2.3.13.1. Структура `FS_INFO_REQUEST`

```c
typedef struct {
    /* пусто */
} FS_INFO_REQUEST;
```

**Размер**: 0 байт
**Назначение**: Запрашивает версию FS, возможности, лимиты и состояния томов.
**Примечание**: Контекст — `OPCODE = 0x00`; request состоит только из общего `SRV_MESSAGE`.

<a id="toc-050c"></a>
###### 2.2.3.13.2. Структура `FS_STAT_REQUEST`

```c
typedef struct {
    u16 path_len;
    u8  path[path_len];
} FS_STAT_REQUEST;
```

**Размер**: `2 + path_len` байт
**Назначение**: Запрашивает тип, атрибуты и размер файла или каталога.
**Примечание**: `path` — абсолютный UTF-8-путь `<volume>:/...`; правила его
проверки приведены в разделе [адресации FS](#fs-paths). Контекст — `OPCODE = 0x01`.

| Поле | Размер | Описание |
|---|---:|---|
| `path_len` | 2 | Длина `path` в байтах UTF-8. |
| `path` | `path_len` | Канонизируемый абсолютный путь файла или каталога. |

<a id="toc-050d"></a>
###### 2.2.3.13.3. Структура `FS_OPEN_REQUEST`

```c
typedef struct {
    u32 open_id;
    u8  kind;
    u8  flags;
    u16 requested_block_size;
    u16 path_len;
    u8  path[path_len];
} FS_OPEN_REQUEST;
```

**Размер**: `10 + path_len` байт
**Назначение**: Открывает поток FILE, TREE или TAR для последующего `FS_READ`.

Поля:

| Поле | Размер | Описание |
|---|---:|---|
| `open_id` | 4 | Ненулевой идентификатор попытки открытия, назначенный клиентом; используется для безопасного повтора OPEN. |
| `kind` | 1 | `1` — FILE, `2` — TREE, `3` — TAR. Другие значения дают `ERR_UNSUPPORTED`. |
| `flags` | 1 | Для TREE bit0 — `RECURSIVE`; для FILE/TAR flags должны быть `0`. Остальные биты дают `ERR_MALFORMED`. |
| `requested_block_size` | 2 | `0` — выбрать максимум сервера; иначе значение от `1` до `max_block_size`. Превышение даёт `ERR_RANGE`. |
| `path_len` | 2 | Длина `path` в байтах UTF-8. |
| `path` | `path_len` | Файл для FILE либо каталог для TREE/TAR. |

<a id="toc-050e"></a>
###### 2.2.3.13.4. Структура `FS_READ_REQUEST`

```c
typedef struct {
    u32 handle;
    u32 block_no;
} FS_READ_REQUEST;
```

**Размер**: 8 байт
**Назначение**: Запрашивает очередной или повторный блок открытого потока.
**Примечание**: `handle` — дескриптор из `FS_OPEN_RESPONSE`; `block_no` начинается
с нуля. Правила последовательности блоков, повторов и EOF приведены в разделе
[жизненного цикла FS](#fs-lifecycle). Контекст — `OPCODE = 0x03`.

| Поле | Размер | Описание |
|---|---:|---|
| `handle` | 4 | Ненулевой дескриптор открытого потока. |
| `block_no` | 4 | Номер очередного блока или повторяемого последнего блока. |

<a id="toc-050f"></a>
###### 2.2.3.13.5. Структура `FS_CLOSE_REQUEST`

```c
typedef struct {
    u32 handle;
} FS_CLOSE_REQUEST;
```

**Размер**: 4 байта
**Назначение**: Закрывает поток и освобождает его backend-дескрипторы.
**Примечание**: Нулевой `handle` даёт `ERR_FS_BAD_HANDLE`; повторное закрытие
ненулевого уже отсутствующего handle идемпотентно. Контекст — `OPCODE = 0x04`.

| Поле | Размер | Описание |
|---|---:|---|
| `handle` | 4 | Ненулевой дескриптор потока, который нужно закрыть. |

<a id="toc-051"></a>
#### 2.2.4. Структура `SRV_RESPONSE_BODY`

```c
typedef struct {
    u16 status;
    union {
        PING_RESPONSE ping;

        MEM_READ_RESPONSE  mem_read;
        MEM_WRITE_RESPONSE mem_write;

        I2C_READ_REG_RESPONSE   i2c_read_reg;
        I2C_WRITE_REG_RESPONSE  i2c_write_reg;
        I2C_POLICY_SET_RESPONSE i2c_policy_set;

        SMI_READ_RESPONSE  smi_read;
        SMI_WRITE_RESPONSE smi_write;

        NOTIFY_SUBSCRIBE_RESPONSE   notify_subscribe;
        NOTIFY_UNSUBSCRIBE_RESPONSE notify_unsubscribe;

        FS_INFO_RESPONSE  fs_info;
        FS_STAT_RESPONSE  fs_stat;
        FS_OPEN_RESPONSE  fs_open;
        FS_READ_RESPONSE  fs_read;
        FS_CLOSE_RESPONSE fs_close;

        PL_SUBSCRIBE_STREAM_RESPONSE   pl_subscribe_stream;
        PL_UNSUBSCRIBE_STREAM_RESPONSE pl_unsubscribe_stream;
    } u;
} SRV_RESPONSE_BODY;
```

**Размер**: переменный; не менее 2 байт (`status`), далее определяется значениями `srv` и `OPCODE`
**Назначение**: Содержит результат обработки запроса (код `status`) и данные ответа конкретной операции.

<a id="toc-052"></a>
##### 2.2.4.1. Поле `status`

**Размер**: 2 байта
**Назначение**: Общий код результата выполнения операции.

Допустимые значения:

| Код | Имя | Семантика |
|---|---|---|
| `0x0000` | `OK` | Операция выполнена успешно |
| `0x0001` | `ERR_MALFORMED` | Некорректный формат или длина сообщения |
| `0x0002` | `ERR_UNSUPPORTED` | Неподдерживаемый `srv`, `OPCODE` или параметр |
| `0x0003` | `ERR_DENIED` | Операция запрещена политикой |
| `0x0004` | `ERR_BUSY` | Временная занятость ресурса |
| `0x0005` | `ERR_TIMEOUT` | Истечение таймаута операции |
| `0x0006` | `ERR_RANGE` | Выход параметров за допустимые границы |
| `0x0007` | `ERR_INTERNAL` | Внутренняя ошибка реализации |
| `0x0100` | `ERR_FS_NOT_FOUND` | Том или объект не найден |
| `0x0101` | `ERR_FS_NOT_READY` | Файловый том не готов |
| `0x0102` | `ERR_FS_BAD_HANDLE` | Дескриптор недействителен или истёк |
| `0x0103` | `ERR_FS_TYPE` | Тип объекта не соответствует режиму открытия |
| `0x0104` | `ERR_FS_IO` | Ошибка файлового ввода-вывода |
| `0x0105` | `ERR_FS_CHANGED` | Обнаружено изменение читаемого файла |

**Примечание**: При `status != OK` тело конкретного ответа должно считаться пустым, если иное явно не определено реализацией.

<a id="toc-053"></a>
##### 2.2.4.2. Структура `PING_RESPONSE`

```c
typedef struct {
    /* пусто */
} PING_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает доступность сервиса `PING`; результат определяется только полем `status`.

<a id="toc-054"></a>
##### 2.2.4.3. Структура `MEM_READ_RESPONSE`

```c
typedef struct {
    u8 data[];
} MEM_READ_RESPONSE;
```

**Размер**: переменный; `count * (width / 8)` байт
**Назначение**: Содержит массив прочитанных данных в порядке выполнения операции чтения.

<a id="toc-055"></a>
##### 2.2.4.4. Структура `MEM_WRITE_RESPONSE`

```c
typedef struct {
    /* пусто */
} MEM_WRITE_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает выполнение операции записи памяти; результат определяется полем `status`.

<a id="toc-056"></a>
##### 2.2.4.5. Структура `I2C_READ_REG_RESPONSE`

```c
typedef struct {
    u8 val;
} I2C_READ_REG_RESPONSE;
```

**Размер**: 1 байт
**Назначение**: Возвращает значение запрошенного I2C-регистра.

<a id="toc-057"></a>
##### 2.2.4.6. Структура `I2C_WRITE_REG_RESPONSE`

```c
typedef struct {
    /* пусто */
} I2C_WRITE_REG_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает выполнение операции записи I2C-регистра.

<a id="toc-058"></a>
##### 2.2.4.7. Структура `I2C_POLICY_SET_RESPONSE`

```c
typedef struct {
    /* пусто */
} I2C_POLICY_SET_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает изменение политики доступа для I2C-устройства.

<a id="toc-059"></a>
##### 2.2.4.8. Структура `SMI_READ_RESPONSE`

```c
typedef struct {
    u16 val16;
} SMI_READ_RESPONSE;
```

**Размер**: 2 байта
**Назначение**: Возвращает значение MDIO-регистра PHY.

<a id="toc-060"></a>
##### 2.2.4.9. Структура `SMI_WRITE_RESPONSE`

```c
typedef struct {
    /* пусто */
} SMI_WRITE_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает выполнение операции записи MDIO-регистра PHY.

<a id="toc-060a"></a>
##### 2.2.4.10. Структура `NOTIFY_SUBSCRIBE_RESPONSE`

```c
typedef struct {
    /* пусто */
} NOTIFY_SUBSCRIBE_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает установку подписки на асинхронные control/state события.

<a id="toc-060b"></a>
##### 2.2.4.11. Структура `NOTIFY_UNSUBSCRIBE_RESPONSE`

```c
typedef struct {
    /* пусто */
} NOTIFY_UNSUBSCRIBE_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает снятие подписки с сервиса `NOTIFY`.

<a id="toc-061"></a>
##### 2.2.4.12. Структура `PL_SUBSCRIBE_STREAM_RESPONSE`

```c
typedef struct {
    /* пусто */
} PL_SUBSCRIBE_STREAM_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает установку подписки на асинхронный стрим.

<a id="toc-062"></a>
##### 2.2.4.13. Структура `PL_UNSUBSCRIBE_STREAM_RESPONSE`

```c
typedef struct {
    /* пусто */
} PL_UNSUBSCRIBE_STREAM_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает снятие подписки на асинхронный стрим.

<a id="toc-063a"></a>
##### 2.2.4.14. Структуры FS-ответов

Эти структуры следуют за общим полем `status` в `SRV_RESPONSE_BODY` и
используются при `srv = 0x07` (`FS`). При ненулевом `status` тело конкретного
ответа отсутствует. Все числовые поля передаются в big-endian.

<a id="toc-063b"></a>
###### 2.2.4.14.1. Структура `FS_INFO_RESPONSE`

```c
typedef struct {
    u8 state;
    u8 name_len;
    u8 name[name_len];
} FS_VOLUME;

typedef struct {
    u16 fs_version;
    u32 capabilities;
    u16 max_block_size;
    u16 max_path_bytes;
    u16 max_depth;
    u16 max_handles;
    u32 idle_timeout_ms;
    u16 volume_count;
    FS_VOLUME volume[];
} FS_INFO_RESPONSE;
```

**Размер**: `20 + Σ(2 + name_len)` байт
**Назначение**: Возвращает версию контракта, capability-маску, лимиты и
доступные логические тома.

| Поле | Размер | Описание |
|---|---:|---|
| `fs_version` | 2 | Версия FS-контракта; текущая версия `1`. |
| `capabilities` | 4 | Биты `FILE`, `TREE`, `TREE_RECURSIVE`, `TAR`, `UTF8_PATHS`, `LIVE_READ` имеют номера `0..5`; остальные зарезервированы. |
| `max_block_size` | 2 | Максимальный `data_len` блока `FS_READ`; в FreeRTOS `3072`. |
| `max_path_bytes` | 2 | Максимальная длина абсолютного пути в UTF-8 без NUL; `511`. |
| `max_depth` | 2 | Максимальная глубина стека каталогов, включая корень; `16`. |
| `max_handles` | 2 | Максимум потоков на соединение; `2`. |
| `idle_timeout_ms` | 4 | Таймаут неактивного потока; `30000` мс. |
| `volume_count` | 2 | Число следующих переменных записей `FS_VOLUME`. |

Значения битов `capabilities`:

| Бит | Имя | Описание |
|---:|---|---|
| 0 | `FILE` | Доступен поток содержимого одного файла. |
| 1 | `TREE` | Доступен поток записей каталога. |
| 2 | `TREE_RECURSIVE` | Поддерживается рекурсивный обход дерева. |
| 3 | `TAR` | Доступен поток несжатого TAR. |
| 4 | `UTF8_PATHS` | Пути и имена в API кодируются UTF-8. |
| 5 | `LIVE_READ` | Чтение идёт из текущей файловой системы, без атомарного snapshot. |

В текущей реализации маска равна `0x0000003F`; неизвестные биты клиент
игнорирует.

Поля `FS_VOLUME`:

| Поле | Размер | Описание |
|---|---:|---|
| `state` | 1 | `0` — not ready, `1` — ready, `2` — unsupported этой сборкой. |
| `name_len` | 1 | Длина имени тома в байтах. |
| `name` | `name_len` | Имя без `:/`, например `flash`; завершающий NUL отсутствует. |

<a id="toc-063c"></a>
###### 2.2.4.14.2. Структура `FS_STAT_RESPONSE`

```c
typedef struct {
    u8  type;
    u8  attributes;
    u64 size;
} FS_STAT_RESPONSE;
```

**Размер**: 10 байт
**Назначение**: Возвращает метаданные объекта.

| Поле | Размер | Описание |
|---|---:|---|
| `type` | 1 | `1` — файл, `2` — каталог. |
| `attributes` | 1 | bit0 `readonly`, bit1 `hidden`, bit2 `system`, bit3 `archive`; остальные биты `0`. |
| `size` | 8 | Размер файла в байтах; для каталога всегда `0`. |

Время изменения объекта протокол FS v1 не передаёт.

<a id="toc-063d"></a>
###### 2.2.4.14.3. Структура `FS_OPEN_RESPONSE`

```c
typedef struct {
    u32 handle;
    u16 block_size;
    u64 total_size;
} FS_OPEN_RESPONSE;
```

**Размер**: 14 байт
**Назначение**: Подтверждает открытие потока и сообщает согласованный размер блока.

| Поле | Размер | Описание |
|---|---:|---|
| `handle` | 4 | Ненулевой непрозрачный дескриптор в пределах TCP-соединения. |
| `block_size` | 2 | Фактически выбранный размер блока `FS_READ`. |
| `total_size` | 8 | Размер FILE; для TREE/TAR — `0xFFFFFFFFFFFFFFFF` (`UNKNOWN`). |

<a id="toc-063e"></a>
###### 2.2.4.14.4. Структура `FS_READ_RESPONSE`

```c
typedef struct {
    u32 handle;
    u32 block_no;
    u8  flags;
    u8  reserved;
    u16 data_len;
    u8  data[data_len];
} FS_READ_RESPONSE;
```

**Размер**: `12 + data_len` байт
**Назначение**: Передаёт очередной фрагмент FILE/TREE/TAR-потока.

| Поле | Размер | Описание |
|---|---:|---|
| `handle` | 4 | Дескриптор потока; должен совпадать с request. |
| `block_no` | 4 | Номер возвращённого блока; должен совпадать с request. |
| `flags` | 1 | Bit0 `EOF`; остальные биты должны быть `0`. |
| `reserved` | 1 | Зарезервировано, всегда `0`. |
| `data_len` | 2 | Число байт в `data`, не больше `block_size`. |
| `data` | `data_len` | Данные потока; записи TREE и границы TAR могут пересекать блоки. |

EOF может сопровождать последний непустой блок. Для пустого FILE/TREE блок
`0` содержит `data_len = 0` и `EOF = 1`; пустой блок без EOF недопустим.

<a id="toc-063f"></a>
###### 2.2.4.14.5. Структура `FS_CLOSE_RESPONSE`

```c
typedef struct {
    /* пусто */
} FS_CLOSE_RESPONSE;
```

**Размер**: 0 байт
**Назначение**: Подтверждает закрытие потока; результат определяется полем `status`.

<a id="toc-064"></a>
#### 2.2.5. Структура `SRV_EVENT_BODY`

```c
typedef struct {
    union {
        NOTIFY_EVENT    notify;
        PL_STREAM_EVENT pl_stream;
    } u;
} SRV_EVENT_BODY;
```

**Размер**: переменный; определяется значениями `srv` и `OPCODE` сообщения-события
**Назначение**: Содержит тело асинхронного события, инициированного сервером.
**Примечание**: Для событий должны быть установлены биты `RESP=0`, `EVENT=1` в поле `op`.

<a id="toc-064a"></a>
##### 2.2.5.1. Структура `NOTIFY_EVENT`

```c
typedef struct {
    u64 time_us;    /* опционально, если WITH_TIMESTAMP=1 */
    u16 ev_type;
    u16 status;
    u8  source;
    u8  bus;
    u8  op_kind;
    u8  reserved;
    u32 arg0;
    u32 arg1;
    u32 arg2;
} NOTIFY_EVENT;
```

**Размер**: 18 байт без `time_us`; 26 байт с `time_us`
**Назначение**: Передаёт структурированное уведомление об изменении состояния системы, попытке регистровой транзакции, её результате или ошибке.
**Примечание**: Контекст использования — `srv = 0x06` (`NOTIFY`), `RESP = 0`, `EVENT = 1`, `OPCODE = 0x10`.

<a id="toc-064b"></a>
###### 2.2.5.1.1. Поле `time_us`

**Размер**: 8 байт
**Назначение**: Микросекундная метка времени события.
**Примечание**: Поле присутствует только если подписка была установлена с `WITH_TIMESTAMP=1`.

<a id="toc-064c"></a>
###### 2.2.5.1.2. Поле `ev_type`

**Размер**: 2 байта
**Назначение**: Тип события control/state уровня.

Допустимые значения:
- `0x0001` — `REG_ATTEMPT`
- `0x0002` — `REG_COMMIT`
- `0x0003` — `REG_DENIED`
- `0x0004` — `STATE_CHANGED`
- `0x0005` — `FAULT`

<a id="toc-064d"></a>
###### 2.2.5.1.3. Поле `status`

**Размер**: 2 байта
**Назначение**: Код результата, связанного с событием.
**Примечание**: Используются те же значения, что и в `SRV_RESPONSE_BODY.status`.

<a id="toc-064e"></a>
###### 2.2.5.1.4. Поле `source`

**Размер**: 1 байт
**Назначение**: Источник, инициировавший действие.

Допустимые значения:
- `0x00` — `TELNET`
- `0x01` — `HOST`
- `0x02` — `DCP`
- `0x03` — `INTERNAL`

<a id="toc-064f"></a>
###### 2.2.5.1.5. Поле `bus`

**Размер**: 1 байт
**Назначение**: Целевая подсистема/шина.

Допустимые значения:
- `0x00` — `I2C`
- `0x01` — `SMI`
- `0x02` — `SPI`
- `0x03` — `UART`
- `0x04` — `SYS`

<a id="toc-064g"></a>
###### 2.2.5.1.6. Поле `op_kind`

**Размер**: 1 байт
**Назначение**: Вид действия, породившего событие.

Допустимые значения:
- `0x00` — `READ`
- `0x01` — `WRITE`
- `0x02` — `POLICY_CHANGE`
- `0x03` — `CONFIG_APPLY`
- `0x04` — `STATE_TOGGLE`

<a id="toc-064h"></a>
###### 2.2.5.1.7. Поля `arg0/arg1/arg2`

**Размер**: 12 байт
**Назначение**: Обобщённые аргументы события, интерпретируемые по контексту `bus + op_kind + ev_type`.

Рекомендуемая интерпретация:
- для `I2C WRITE`: `arg0 = addr7`, `arg1 = reg`, `arg2 = val`
- для `SMI WRITE`: `arg0 = phy`, `arg1 = reg`, `arg2 = val16`
- для системных событий: значения определяются реализацией
**Примечание**: Все поля передаются в big-endian.

<a id="toc-065"></a>
##### 2.2.5.2. Структура `PL_STREAM_EVENT`

```c
typedef struct {
    u64 time_us;    /* опционально, если WITH_TIMESTAMP=1 */
    u8  ev_flags;
    u32 lost_delta;
    u16 data_len;
    u8  data[];
} PL_STREAM_EVENT;
```

**Размер**: `1 + 4 + 2 + data_len` байт без `time_us`; `8 + 1 + 4 + 2 + data_len` байт с `time_us`
**Назначение**: Передаёт порцию данных стрима выбранного PL-сервиса (`I2C`, `SMI`, `SPI`, `UART`) и диагностику потерь.

<a id="toc-066"></a>
###### 2.2.5.2.1. Поле `ev_flags`

**Размер**: 1 байт
**Назначение**: Флаги состояния стрима для текущего события.

Битовое представление:
- `bit0` — `OVERFLOW` (обнаружена потеря данных с прошлого события)
- `bit7..1` — зарезервированы (должны быть равны 0)

<a id="toc-067"></a>
###### 2.2.5.2.2. Поле `lost_delta`

**Размер**: 4 байта
**Назначение**: Количество потерянных единиц данных с момента предыдущего события (или с момента подписки/сброса счётчиков).

<a id="toc-068"></a>
###### 2.2.5.2.3. Поле `data_len`

**Размер**: 2 байта
**Назначение**: Длина поля `data` в байтах.

**Примечание**: При использовании режима `RAW_WORDS` значение `data_len` должно быть кратно 4.

<a id="fs-service"></a>
### 2.3. Сервис FS: адресация, потоки и жизненный цикл

Сервис `FS` (`srv=0x07`) предоставляет внешнему клиенту потоковое чтение
файловой системы. В FreeRTOS он использует тома `sd`, `flash` и `sd-pl`; в
конкретной сборке состояние каждого тома объявляется через `FS_INFO`. Запись,
удаление и форматирование в FS v1 отсутствуют. В Neutrino backend FS пока не
подключён и возвращает `ERR_UNSUPPORTED`.

Каждый request получает один response с тем же `seq` и `op = opcode | 0x80`.
FS не создаёт собственных events; события других сервисов могут приходить между
ответами и обрабатываются независимо.

<a id="fs-paths"></a>
#### 2.3.1. Адресация и ограничения

Поля `path_len`/`path` используют абсолютный путь `<volume>:/<components>`;
длина задаётся в байтах UTF-8, завершающий NUL не передаётся. Примеры:
`flash:/config/network.json` и `sd:/`. Имена томов состоят из строчных ASCII-
букв, цифр и дефиса, длина имени — 1..15 байт. Числовые FatFs-пути наружу не
выдаются.

Конечный `/` каталога нормализуется. Компоненты `.`, `..`, пустые промежуточные
компоненты, NUL, управляющие символы U+0000..001F/U+007F и символы
`\\ : * ? " < > |` внутри компонентов запрещены (`ERR_MALFORMED`). Некорректный
UTF-8 и превышение `max_path_bytes` дают ошибку. Путь не усекается и имена не
пропускаются молча.

FreeRTOS-адаптер проверяет готовность тома и не вызывает автоматическое
монтирование или форматирование. Если символ не представим в кодировке
носителя/API, возвращается `ERR_RANGE`. Атрибуты объекта и состояние тома
описываются в `FS_STAT_RESPONSE` и `FS_INFO_RESPONSE` соответственно.

<a id="fs-streams"></a>
#### 2.3.2. Потоки FILE, TREE и TAR

`FILE` передаёт исходные байты файла последовательно; после EOF число полученных
байт обязано совпасть с `total_size`. Произвольного seek нет.

`TREE` без `RECURSIVE` перечисляет непосредственных детей каталога. С
`RECURSIVE` выполняется обход в глубину: каталог предшествует потомкам, порядок
соседей определяется файловой системой, корень в результат не включается.
Пустые каталоги представлены отдельными записями. Поток состоит из
`FS_TREE_ENTRY`, причём запись может пересекать границу `FS_READ`-блока:

```c
typedef struct {
    u16 entry_len;
    u8  type;
    u8  reserved;
    u64 size;
    u16 path_len;
    u8  path[path_len];
} FS_TREE_ENTRY;
```

**Размер**: `14 + path_len` байт.

| Поле | Размер | Описание |
|---|---:|---|
| `entry_len` | 2 | Полный размер записи, включая это поле; используется для сборки записей из блоков. |
| `type` | 1 | `1` — файл, `2` — каталог. |
| `reserved` | 1 | Всегда `0`. |
| `size` | 8 | Размер файла; для каталога всегда `0`. |
| `path_len` | 2 | Длина относительного UTF-8-пути без конечного `/`. |
| `path` | `path_len` | Путь относительно корня обхода, без пустых компонентов, `.` и `..`. |

EOF внутри незавершённой `FS_TREE_ENTRY` означает ошибку потока.

`TAR` всегда рекурсивен и передаёт один несжатый архив с относительными путями,
файлами и каталогами, включая пустые. Используется ustar; для длинных или
не-ASCII имён и больших размеров добавляются локальные PAX-заголовки `path` и
`size`. UID/GID/mtime равны нулю, mode файлов — `0644`, каталогов — `0755`.
Архив завершается двумя нулевыми блоками по 512 байт; границы TAR не обязаны
совпадать с границами DCP2-блоков.

Обход и формирование данных выполняются пошагово: полный список или архив не
накапливается в памяти устройства и не записывается во временный файл на flash.

<a id="fs-lifecycle"></a>
#### 2.3.3. Дескрипторы, повторы и ошибки

`open_id`, `handle`, `block_no` и `seq` имеют разные области действия:

- `seq` связывает один request/response; повтор использует новый `seq`.
- `open_id` назначает клиент. Повтор OPEN с тем же идентификатором и параметрами
  возвращает тот же поток; изменение параметров даёт `ERR_MALFORMED`.
- `handle` принадлежит одному TCP-соединению. Сервер не переиспользует его номер
  в этом соединении. CLOSE ненулевого отсутствующего handle идемпотентен.
- `block_no` начинается с `0`. Разрешены следующий блок и повтор последнего;
  другие номера дают `ERR_RANGE` без продвижения потока.

Последний успешный блок, включая EOF, кэшируется до CLOSE или таймаута; повтор
возвращает те же байты. После `idle_timeout_ms` освобождаются backend-объекты.
При разрыве TCP освобождаются все потоки. На соединение допускается максимум два
handle.

Клиент может повторять временные ошибки и timeout с новым `seq`; для
`ERR_FS_NOT_READY` это также допустимо. Частично принятый кадр сохраняется до
полного чтения, поздние ответы со старым `seq` игнорируются. Ошибка генерации
блока становится терминальной для handle и повторяется до CLOSE.

Чтение выполняется без атомарного snapshot. Для каждого файла проверяются размер
и доступная отметка изменения; досрочный EOF, подмена или удаление дают
`ERR_FS_CHANGED`. Изменение того же размера с той же отметкой времени может быть
необнаружено; параллельные изменения каталога также не гарантируются.

<a id="fs-examples"></a>
#### 2.3.4. Примеры обмена

Рекурсивное перечисление конфигурации:

```text
FS_INFO(seq=1) -> OK, fs_version=1, volumes=[sd, flash, sd-pl], limits...
FS_OPEN(seq=2, open_id=1, kind=TREE, flags=RECURSIVE,
        requested_block_size=3072, path="flash:/config/")
  -> OK, handle=1, block_size=3072, total_size=UNKNOWN
FS_READ(seq=3, handle=1, block_no=0) -> OK, data..., EOF=0
FS_READ(seq=4, handle=1, block_no=1) -> OK, data..., EOF=1
FS_CLOSE(seq=5, handle=1) -> OK
```

При повторе READ используется новый `seq`, но тот же `handle` и `block_no`.
При потере ответа OPEN повторяются `open_id` и остальные параметры. Повтор CLOSE
безопасен.

Полный `FS_OPEN` frame для `seq=1`, `open_id=1`, `TREE|RECURSIVE`,
`block_size=3072`, пути `flash:/config/`:

```text
44 43 50 32 00 02 00 1C  07 02 00 01
00 00 00 01 02 01 0C 00  00 0E
66 6C 61 73 68 3A 2F 63  6F 6E 66 69 67 2F
```

Пустой успешный FILE-блок для `handle=1`, `block_no=0`, `seq=2`:

```text
44 43 50 32 00 02 00 12  07 83 00 02 00 00
00 00 00 01 00 00 00 00  01 00 00 00
```

Host-тесты дополнительно проверяют многоблочные файлы, пустые объекты,
рекурсивный TREE с пересечением границ блоков, TAR/PAX, повторы OPEN/READ/CLOSE,
timeout, изменение файла, лимиты дескрипторов и освобождение состояния при
разрыве TCP.
